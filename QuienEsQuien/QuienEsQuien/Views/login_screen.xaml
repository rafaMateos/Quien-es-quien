<Page
    x:Class="QuienEsQuien.Views.login_screen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuienEsQuien.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:QuienEsQuien.Viewmodel"
    Background="#536DFE">


    <Page.DataContext>
        <vm:viewModel/>
    </Page.DataContext>

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="10">
        <TextBlock > Bienvenido, introduce tu nick: </TextBlock>
        <TextBlock Text="{Binding nickJugador, Mode=TwoWay}"/>
        <TextBox Name="txtNickJugador" Text="{Binding nickJugador, Mode=TwoWay}"/>
        <Button Content="Entrar"
                 Click="HyperButton_Click"
                 HorizontalAlignment="Center"/>
    </StackPanel>
</Page>
